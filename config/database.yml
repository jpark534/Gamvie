default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: match_a_movie_development

test:
  <<: *default
  database: match_a_movie_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>